# QA v1.2.10

## 发布一致性

- [ ] `VERSION` 为 `1.2.10`。
- [ ] Xcode `MARKETING_VERSION=1.2.10`、`CURRENT_PROJECT_VERSION=1210`。
- [ ] portable runtime `appVersion=1.2.10`、`buildNumber=1210`。
- [ ] `python3 scripts/validate-source.py` 通过。
- [ ] `python3 scripts/release-preflight.py --strict-macos` 在 GitHub `macos-15` arm64 runner 通过。

## GitHub `.pkg` 状态栏回归

- [ ] 默认“图标 + 状态 + 网速”时，macOS 状态栏能同时显示图标与上下两行实时网速，不再只剩图标。
- [ ] 关闭/打开“图标 / 状态 / 网速”任一开关后，状态栏宽度与内容立即更新；三个选项不能同时关闭。
- [ ] 状态栏下拉菜单背景比 v1.2.9 更柔和，不出现大面积近纯黑。
- [ ] 菜单低于一屏时自然全部展开，不出现滚动容器。
- [ ] 菜单超过一屏时可通过鼠标滚轮/触控板上下滚动，但右侧不显示系统粗滚动条。
- [ ] 每个代理组行右侧明确显示当前 `now` 选择；重新打开菜单时会刷新外部变更后的选择。
- [ ] 从菜单切换线路后，对应代理组当前选择立即更新。

## 主窗口与稳定性回归

- [ ] 主窗口右侧内容区为较柔和的深灰蓝背景，卡片仍保持清晰层级。
- [ ] 关闭主窗口后从状态栏重新打开，窗口仍可正常移动。
- [ ] 正常状态栏不进入“恢复模式：状态栏渲染已降级”。
- [ ] v1.2.9 的超屏自适应菜单高度仍正常。
