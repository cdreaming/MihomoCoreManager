# QA v1.2.4

## 代理排序

- [ ] 代理组“默认”按 `/group` 返回顺序显示。
- [ ] 代理组“延时”按当前线路的有效延时升序，未知/超时靠后。
- [ ] 代理组“质量”优先存活、稳定、低抖动节点。
- [ ] 代理组“名字”按名称排序。
- [ ] 代理列表四种排序均可独立切换。
- [ ] 代理列表“默认”保持当前组 `all` 顺序。

## 当前组测速

- [ ] 点击“测速当前组”只调用 `/group/{group}/delay`。
- [ ] 代理组名称含空格/斜杠时 URL path segment 编码正确。
- [ ] 测速完成后当前组显示结果。
- [ ] 同名节点存在其它组时，其它组同步显示该测速结果。
- [ ] 超时结果显示为“超时”，未知结果显示为 `-- ms`。
- [ ] 测速不会触发其它代理组批量测速。

## 状态栏

- [ ] 可见代理组均位于状态栏顶层。
- [ ] 每个代理组子菜单均有“测速此组”。
- [ ] Selector / URLTest / Fallback 可切换线路。
- [ ] 当前线路显示选中标记。
- [ ] 状态栏节点显示最新测速结果。
- [ ] 切换服务器后状态栏代理组同步刷新。

## 回归

- [ ] v1.2.3 Controller Secret 独立认证继续可用。
- [ ] 规则 / 全局 / 直连切换继续可用。
- [ ] Core 控制、订阅、日志、升级、状态栏网速不回归。
- [ ] `CGO_ENABLED=0 go test ./...`
- [ ] 全部 Swift 文件通过 `swiftc -frontend -parse`
- [ ] portable installer 是 Mach-O arm64。

## Hotfix 回归

- [ ] “默认”代理组顺序与 `GLOBAL.all` / config.yaml 顺序一致，不能使用 `/group` 的随机 map 遍历顺序。
- [ ] 组测速返回后不进行第二次远程 `/proxies` 请求也立即显示延时。
- [ ] 节点 `history` 为空、只有 `extra[testUrl]` 时仍显示延时。
- [ ] 状态栏根菜单打开时不访问远程 `/proxies`/`/group`。
- [ ] 状态栏代理数据来自本地原子快照文件。
- [ ] 只有进入某个代理组子菜单时才创建该组线路项。
- [ ] 状态栏“测速此组”不会阻塞菜单 5 秒。
- [ ] 状态栏线路切换不会同步等待远程 PUT。
- [ ] 代理组项前没有额外 SF Symbol。
- [ ] 状态栏线路可显示显式测速、extra 或 history 延时。

- [ ] `/proxies` 节点只有 `extra[testUrl] = {alive, history:[...]}`、顶层 `history` 为空时，代理页和状态栏具体线路仍显示延时。

- [ ] 当策略组 `all` 中的具体线路只存在于 `/providers/proxies`、不在 `/proxies` 时，代理页和状态栏仍显示该线路及其延时。
- [ ] 组成员是嵌套策略组时，延时递归跟随 `now` 到最终叶子节点。
- [ ] 指定测试 URL 返回 0、provider 的其它测试 URL 有正延时时，具体线路显示正延时而不是空值/超时。

- [ ] Controller 连续返回 Cloudflare HTTP 530 / Error 1033 时，GET 请求执行有限次短重试。
- [ ] 有最近一次代理快照时，530 不清空代理页，仍显示代理组、具体线路和延时。
- [ ] 530 错误提示不包含整段 Cloudflare JSON/HTML，只显示简洁 Tunnel 断开提示。
- [ ] 代理页使用 stale 快照时不再额外请求 `/configs`，避免断线时二次等待。
- [ ] 状态栏在 Tunnel 断开期间继续读取最后成功的本地 `Runtime/proxies.json`。
- [ ] 线路 PUT 已成功、后续 GET 恰好遇到 Tunnel 断开时，界面保留刚刚选择的线路，不回滚到旧快照。
