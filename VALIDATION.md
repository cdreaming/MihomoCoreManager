# Validation Report — v1.1.1 macOS App

- App 版本统一为 `1.1.1`，Xcode build number 为 `111`。
- GitHub `macOS Release` 继续默认执行 `scripts/build-release.sh --unsigned`。
- 原生 `MenuBarExtra` 标签已改为更紧凑的横向布局，并统一固定高度以改善垂直居中。
- 双行实时网速改用 SF Symbols 箭头与更紧凑的两行排版。
- Release 自动发布、源码完整性门禁、arm64 版本元数据、API/菜单/设置关键路径保持有效。
